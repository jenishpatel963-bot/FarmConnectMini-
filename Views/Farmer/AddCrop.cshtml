@model FarmConnectMini.Models.Crop
@{
    ViewData["Title"] = "Add New Crop";
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-7">
            <div class="card border-0 shadow-lg rounded-4">
                <div class="card-header bg-success text-white text-center rounded-top-4">
                    <h3 class="fw-bold mb-0">🌾 Add New Crop</h3>
                </div>
                <div class="card-body p-4 bg-light">
                    <form asp-action="AddCrop" method="post" class="needs-validation">
                        <div class="form-floating mb-3">
                            <input asp-for="Name" class="form-control rounded-3" placeholder="Enter crop name" required />
                            <label for="Name">Crop Name</label>
                        </div>

                        <div class="form-floating mb-3">
                            <input asp-for="Price" type="number" step="0.01" class="form-control rounded-3" placeholder="Enter price" required />
                            <label for="Price">Price (₹)</label>
                        </div>

                        <div class="form-floating mb-4">
                            <input asp-for="Quantity" type="number" class="form-control rounded-3" placeholder="Enter quantity" required />
                            <label for="Quantity">Quantity (kg)</label>
                        </div>

                        <div class="d-flex justify-content-between">
                            <a asp-action="Index" class="btn btn-outline-secondary px-4 py-2 rounded-pill">← Back</a>
                            <button type="submit" class="btn btn-success px-4 py-2 rounded-pill shadow">💾 Save Crop</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
